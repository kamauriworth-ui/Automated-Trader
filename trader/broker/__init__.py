"""Everything to do with the broker (the company that holds the account and executes trades).

The rest of the app only uses the plain data classes in `models.py` and the
`Broker` description in `base.py`. Only `alpaca_paper.py` knows Alpaca exists.
"""
